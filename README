AdvancedHttp
============

AdvancedHttp provides a convenient Ruby API for making HTTP requests.

Features:

  * GET, PUT and POST HTTP requests
  * support for  HTTP Basic and Digest authentication
  * pluggable unmarshallers for parsing response bodies


Example
=======

Get html page

    require 'advanced_http'
    http = AdvancedHttp::HttpAccessor.new
    resp = http.resource('http://rubyforge.org').get(:accept => 'text/html')
    puts resp.body

Get page requiring HTTP Authentication

    require 'advanced_http'
    class AuthInfoProvider
      def authenication_info(realm)
        ['my_account', 'my-super-secert-password']
      end
    end

    http = AdvancedHttp::HttpAccessor.new(:authentication_info_provider => AuthInfoProvider.new)
    resp = http.resource('http://internal-app/').get(:accept => 'text/html')
    puts resp.body


Copyright (c) 2007 Absolute Performance, Inc, released under the GNU Lesser General Public License.
